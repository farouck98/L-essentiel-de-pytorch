import torch

#tensor généré avec des valeurs aléatoires
a5 = torch.rand(2,2)
print(f"a5={a5}")

#tensor à 3 valeurs initialisés avec les valeurs zéros
a6 = torch.zeros(3)
print(f"a6={a6}")

#tensor à 3 valeurs initialisés avec les valeurs UN
a7 = torch.ones(3)
print(f"a7={a7}")